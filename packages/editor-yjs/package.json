{
  "name": "@repo/editor-yjs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Runtime-neutral Yjs document layer for editor block-content Y.Doc contexts, updates, metadata, origins, and observability. No DOM dependency.",
  "main": "./dist/api/index.js",
  "types": "./src/api/index.ts",
  "exports": {
    ".": {
      "types": "./src/api/index.ts",
      "development": "./src/api/index.ts",
      "import": "./dist/api/index.js"
    },
    "./checkpoint-format": {
      "types": "./src/block-content/checkpoint-format.ts",
      "development": "./src/block-content/checkpoint-format.ts",
      "import": "./dist/block-content/checkpoint-format.js"
    }
  },
  "scripts": {
    "build": "pnpm run clean && tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "check-types:source": "tsc --noEmit -p tsconfig.json",
    "check-types:tests": "tsc --noEmit -p tsconfig.test.json",
    "check-types": "pnpm run check-types:source && pnpm run check-types:tests",
    "typecheck": "pnpm run check-types",
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@repo/editor-core": "workspace:*",
    "yjs": "^13.6.24"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "@vitest/coverage-v8": "^4.0.15",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^4.0.8"
  }
}
