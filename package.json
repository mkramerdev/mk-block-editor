{
  "name": "mk-block-editor",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "pnpm run dev:prepare && turbo watch dev:consumer --ui=stream",
    "dev:prepare": "turbo run dev:prepare --ui=stream",
    "dev:realtime": "turbo run dev:prepare --filter=@repo/editor-realtime --ui=stream && turbo watch dev:consumer --filter=@repo/editor-realtime --ui=stream",
    "dev:playground-react": "turbo run dev:prepare --filter=playground-react --ui=stream && turbo watch dev:consumer --filter=playground-react --ui=stream",
    "dev:playground": "turbo run dev:prepare --filter=playground --ui=stream && turbo watch dev:consumer --filter=playground --ui=stream",
    "test:development-contracts": "turbo run dev:prepare --filter=playground-react --filter=@repo/editor-realtime --ui=stream && node --test scripts/development-contracts.test.mjs",
    "test:development-clean-start": "node --test scripts/development-clean-start.test.mjs",
    "db:reset:first-draft": "turbo run db:reset --filter=@repo/editor-first-draft",
    "db:seed:first-draft": "turbo run db:seed --filter=@repo/editor-first-draft",
    "lint": "pnpm run format:check && turbo run lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.10.10",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=22.13"
  }
}
